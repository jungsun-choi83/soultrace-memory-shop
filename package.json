{
  "name": "soultrace-memory-shop",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "SoulTrace Memory Shop — email-verified letter selection, local demo and storefront preview",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.mjs && node --check src/archive-flow.mjs && node --check src/archive-api.mjs && node --check src/i18n.mjs && node --check server/app.mjs && npm test && npm run build",
    "start": "node scripts/serve.mjs --dist",
    "keygen": "node scripts/keygen.mjs"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
