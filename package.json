{
  "name": "soultrace-memory-shop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SoulTrace personalized memory-goods storefront — safe, dependency-free demo",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs --dist",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.mjs && node --check src/core.mjs && node --check src/catalog.mjs && node --check src/i18n.mjs && npm test && npm run build"
  },
  "engines": { "node": ">=20" }
}
