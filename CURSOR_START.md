# Cursor에서 이어서 작업할 프롬프트

아래 내용은 **프로젝트 폴더를 연 다음** Cursor Agent에 전달합니다.

---

이 프로젝트는 SoulTrace 메모리 상품 자사몰 프런트엔드 MVP다.
먼저 README.md, docs/INTEGRATION.md, docs/LAUNCH_CHECKLIST.md 및 src/ 파일을 읽고,
`npm run check`를 실행해 현재 동작을 확인하라.

기존 SoulTrace 코드는 현재 폴더에 없다. 기존 코드가 연결되어 있지 않으면 API·인증·DB 테이블을
있다고 가정하지 말고 정확한 저장소 경로와 API 계약을 요청하라. 현재 예시 로그인/주문을
실제 동작으로 설명하지 말 것.

유지할 것:
- 한국어 프런트엔드, 아이보리·세이지의 에디토리얼 디자인, PC/모바일 레이아웃.
- letter 24,900원 / nfc 9,900원 / minibook 49,900원. 카탈로그 표시 순서는 nfc → letter → minibook.
- 사용자 요청 없이 프레임워크 전체를 교체하지 말 것. 기존 앱이 React/Next.js라면 확인된
  라우팅·인증·컴포넌트 규칙에 맞춰 화면을 이식하되 디자인과 테스트를 유지할 것.
- 업로드 이미지·편지·배송 정보를 localStorage, URL, 오류 로그, 분석 이벤트에 넣지 말 것.
- 서버가 검증한 정보 없이는 실결제 버튼을 활성화하지 말 것.
- 배송비 미정은 무료가 아니다. 미확정 상품 사양·후기·재고·사업자 정보는 지어내지 말 것.

실서비스 연결 순서:
1. 실제 SoulTrace 인증, 사용자 소유 아카이브 조회, 저장소 권한을 확인한다.
2. 서버 카탈로그·유효한 옵션·재고·배송비·세금이 포함된 견적 API를 작성한다.
3. 사용자 소유 콘텐츠 ID만 참조하는 주문 초안을 서버에 저장하고 견적 만료를 검증한다.
4. 해당 PG의 공식 문서와 실제 SDK 버전을 확인한 뒤 결제 생성·확인·웹훅을 붙인다.
5. 결제금액/통화/주문상태, 멱등성, 웹훅 재전송, 취소·실패·환불 테스트를 작성한다.
6. 개인정보 최종 고지·보관/삭제·동의·사업자/배송 정보를 확인하고 주문 조회를 붙인다.

각 단계마다 변경 파일, 실행한 테스트, 실제 성공/실패, 미구현 항목을 기록하라.
서버 키를 프런트엔드 코드에 넣지 말고 자동 PG 청구·실고객 이메일·실제 주문은
명시적으로 승인된 테스트 환경에서만 수행하라.

---
